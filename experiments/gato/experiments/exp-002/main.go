// Package exp002 — EXP-002: Audio Resampling benchmark entry point.
// Run `go test -bench=. -benchmem -count=5` for benchmarks.
// Run `go test -v` for correctness tests.
package main

func main() {}
